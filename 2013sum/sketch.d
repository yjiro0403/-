import std.stdio, std.algorithm, std.array, std.string;
import std.file, std.typecons;

void main()
{
    string test = "a1f";
    
    //if(is(typeid(test) : int))
        assert(isNumeric("d3e")==false);
}
